# AGENTS.md · WisdoMark 工作约定

> 给所有在此仓库上工作的 AI / 协作者的交接文档。开工前先读完本节。
> 完整任务书在仓库外（用户本地维护），此处只固化**不可动摇的约定**。

---

## 一、项目定位

本地运行的「学习资料消化器」：读浏览器收藏夹或粘贴链接 → AI 自动分类归档 → 生成一句话摘要 + 3 个核心观点 → 基于用户自身状况提炼「可行动的下一步」。

**核心卖点**：不是「又一个剪藏 demo」，而是**带评测与护栏的本地 AI 工具**。差异化来自「画像推断 → 可行动清单」和「可量化的失败率评测」。

---

## 二、已拍板的架构决策（不要重新纠结）

| 决策点 | 结论 |
|---|---|
| 产品形态 | 纯 Chrome MV3 插件，**无独立后端** |
| 桌面 exe | **放弃**（逆解析书签文件脆、打包重） |
| 后端语言 | **不要**（FastAPI / Next.js 都不需要），业务逻辑全在 service worker |
| 云端 | **本地优先，零云零费**；云端模型是后期可选项，不是必需 |
| 数据库 | `chrome.storage.local` + IndexedDB；**不用** Neon / PG |
| 部署 | 暂不部署，走本地交付（README + 录屏 + 开源仓库即展示物） |
| 参考项目 Karakeep | **只读设计，禁止复制源码**（AGPL 传染条款） |
| 内容源 | 只做**网页文章**；B 站字幕需 cookie + 接口已停更，**暂缓** |
| 抓正文路径 | **当前页注入**：`chrome.scripting` 运行时注入 content script，抓到 DOM 后再解析。**且必须等正文渲染稳定才取**（SPA 的 `complete` 事件早于正文渲染，见下文专节） |
| 主机权限 | `host_permissions` 含 `*://*/*`（见下方说明，`activeTab` 已证明不可靠） |
| 评测与插件的关系 | **单一来源**：prompt / 分类体系 / 校验规则放 `shared/`，插件与 Python 脚本读同一份；Python 只做批量调用 + 打分，不重写业务逻辑 |
| 抓正文实现 | 阶段 1 用原生 DOM 提取；Readability 后续再评估 |
| 模型 | 本地 `qwen3:8b` 起步 |

---

## 三、硬约束

| 项 | 内容 |
|---|---|
| 时间 | 每周可支配 10–15 小时，投递窗口 2026-11-20 起 |
| 提交 | commit message **一律中文**；**改动不论大小都单独提交**，绝不攒批。**提交完不要自动 push**（见下节） |
| 推送 | **由用户每天工作结束时口头指令触发** —— 他说可以推了才执行。平时只 commit，不 push |
| 推送后 | `git ls-remote <remote> main` 与本地一致 |
| 依赖 | 不为了解决问题而装新软件，优先零安装 / 轻依赖；新增依赖须先说明为什么必须加。**唯一的例外是 `vendor/tesseract/`（离线 OCR 引擎）**：它必须离线可用，所以整个引擎随仓库走，用户侧零安装 |
| 禁止改动 | **不得改动 `D:\PROJECT\NaviRAG` 的任何文件**（只读参考可以） |
| 语言 | 全中文交流；**代码注释用中文** |

### 双远端推送（GitHub + Gitee）

本仓库有 **两个远端**：`github`（MaPleooo9/WisdoMark）与 `gitee`（iKnowuHateme/wisdo-mark）。
`main` 的上游跟踪只指向其中一个（目前是 `gitee`）。

**因此：`git status` 显示 up to date 只能证明本地与上游那个远端一致，不能证明双端同步。**
推的时候要**显式**推两个远端，并以 `git ls-remote` 做校验：

```bash
git push github main
git push gitee main
git ls-remote --heads github main
git ls-remote --heads gitee main   # 两行哈希应与 git rev-parse HEAD 相同
```

#### 推送时机由用户控制（2026-09-15 起）

- **每次改动照常单独 commit** —— 本地提交不受任何影响，历史依然是一条改动一条提交
- **但不要自动 push。** 用户担心高频推送被 GitHub / Gitee 判定为滥用服务
- 每天工作结束时用户会说一声，**那时才执行 push + 哈希校验**
- 所以中途 `git status` 显示「领先远端 N 个提交」是**正常状态，不是漏推**，
  不要自作主张补推，也不用反复提醒

> 一句话：**commit 是即时的，push 是按天的**。批量只体现在推送这个动作上，
> 提交历史依然是细粒度的 —— 两者不冲突。


---

### 新增 `chrome.*` API 时必须同步 manifest 权限

实测踩过（2026-09-13）：`getRecentBookmarks()` 调用 `chrome.bookmarks.getRecent`，
但 manifest 的 `permissions` 从阶段 0 起就只有 `sidePanel / tabs / scripting / storage` ——
**`bookmarks` 从来没加过**。翻 git 历史确认 8 个提交里这一行没变过，
也就是说「读取最近收藏」这个入口**从写下那天起就没成功过**，直到用户点它才暴露。

后果的形态值得记住：**缺权限时 `chrome.<api>` 是 `undefined`，不是抛「权限不足」**，
所以报错会指向一个和权限毫不相干的地方：

```
读取失败：读取收藏夹失败：Cannot read properties of undefined (reading 'getRecent')
```

用户既看不懂，也无从下手。规矩：

- 新增任何 `chrome.<api>` 调用，先确认它在 manifest 的 `permissions` / `host_permissions` 里有对应项
- 调用点自己做存在性判断（`chrome.bookmarks?.getRecent`），缺失时返回**能照着做**的提示
  （「到 `edge://extensions` 重新加载本扩展」），不要把 undefined 的天书丢给用户
- 核对办法：`grep -rho "chrome\.[a-zA-Z]*" src/ | sort -u` 与 manifest 的 permissions 对一遍
- 补权限后必须**升版本号**并让用户重新加载 —— 新增权限时 Edge 会请求一次授权，
  版本号不变用户无法确认自己加载的是新版本

---

### 运行前置：`OLLAMA_ORIGINS` 必须放行扩展来源

阶段 1 实测踩到：插件状态灯是绿的（探活成功），一消化就报 `HTTP 403`，5ms 返回、模型根本没被调用。

根因是 Ollama 的 **Origin 白名单**，不是插件代码问题：

| 事实 | 说明 |
|---|---|
| Ollama 默认只放行 `localhost` / `127.0.0.1` 等来源 | `chrome-extension://` 不在其中 |
| 它拦的是**带 `Origin` 头**的请求 | 实测：`GET /api/tags` 不带 Origin → 200；带 Origin → 403 |
| 探活为什么能过 | 扩展发的 GET 是「简单请求」，浏览器不附加 Origin |
| 调模型为什么必挂 | `POST` + `Content-Type: application/json` 触发跨源语义，浏览器附上 Origin → 403 |

**表现具有欺骗性：状态灯绿 + 一用就失败**，极易误判成模型或代码问题。

解法（一次性环境配置，非代码改动）：

```bash
setx OLLAMA_ORIGINS "chrome-extension://*"
# 然后完全退出 Ollama（托盘右键退出）再重新打开
```

`OLLAMA_ORIGINS` 是**追加**到默认白名单，不会破坏 localhost 访问。

代码侧已做的配合：`src/background/llm.js` 的 `describeHttpFailure()` 会把 403 直接翻译成这句可操作提示，
不让用户只看到一个裸状态码。**以后新增 Ollama 调用路径时，错误都要过这个函数。**

> 备选方案（未采用，仅记录）：用 `declarativeNetRequest` 删掉请求的 `Origin` 头可绕开，但需要额外权限且属于钻空子，不如环境变量正规。

---

### 护栏边界：宽进严出，只拦「没有正文主体」的页面

阶段 1 验收时用户反馈：**应用推荐、游戏更新公告这类内容被拒收**（"模型判定这篇文章无法消化"）。
用户的原话是要点的核心：*「有些时候文章就是推荐应用和该应用的使用，或者是某个游戏的更新公告，太长了我不愿意看完才交给你」*
—— 这类内容恰恰是**最该被压缩**的，却被判为不可消化。

排查出两个原因，都是设计缺陷而非代码 bug：

| # | 原因 | 表现 |
|---|---|---|
| 1 | prompt 把输出定义成「摘要 + 3 个**核心观点**」 | 推荐 / 公告类内容里不存在"观点"，模型找不到就退回 `ok:false` |
| 2 | **`readerProfile` 被模型当成了内容过滤器** | 实测输出过 `ok:false — 正文内容为游戏更新公告，与AI Agent/全栈方向求职无关` |

**第 2 条更危险**：它会让收藏夹里所有非 AI / 技术主题的内容全部被误杀，与产品定位（收藏消化助手，不是 AI 文章筛选器）直接冲突。

**核心原则：误拒比误纳更伤用户。** 用户已经付出「点按钮 + 等 5~15 秒」的成本，返回「无法消化」是零收益。

**结论（已固化进 `shared/prompt.json` v1.1.0）**：

- `ok:false` **仅**用于「正文根本不是内容主体」：登录页、注册页、验证码 / 权限校验页、错误页 / 404、访问被拒绝、纯导航或目录页、Cookie 与隐私提示页、正文为空或只有寥寥几个字。
- 以下**都不是**判 false 的理由：主题与读者专业方向无关、体裁不是深度文章、信息量偏少。
- `readerProfile` 只用于决定「哪些信息值得强调、怎么组织更好懂」，**明确不作过滤条件**。
- `points` 的 3 条语义随体裁变化（核心观点 / 关键事实 / 关键变化 / 关键步骤 / 具体条目）。

**回归用例（改 prompt 必须重跑）**：应用推荐、版本更新公告、工具教程、纯营销软文、登录页。
期望：前四类 `ok:true`，登录页 `ok:false`。

跑法：临时 Node 脚本直接请求 `/api/chat`（读 `shared/prompt.json` 渲染模板，与插件走同一份 prompt）。
**脚本放仓库外的临时目录、跑完即删，不进仓库**——它是验证手段，不是项目资产。

→ **「误拒率」应当成为阶段 3 评测集的指标之一**，与分类准确率、JSON 失败率并列。

---

### 抓正文：必须等页面渲染稳定，`complete` 不等于渲染完

阶段 1 验收时用户反馈：**两篇内容完整的小黑盒文章都被判「没有可消化的正文」**。

排查结论：**不是模型错，是我们喂给它的东西不对。**

| 证据 | 结果 |
|---|---|
| `curl` 该 URL | HTTP 200，**只有 2698 字节**，`<title>小黑盒 - 玩家高能聚集地</title>`（首页标题），正文关键词 0 次 |
| Edge 无头渲染该 URL 后的可见文本 | **只有 287 字**：`首页 社区 小黑盒加速器 … 登录 返回 … 立即下载小黑盒APP` —— 全是导航和页脚 |
| 那次消化的耗时 | 533 ms（正常消化要 5~15 秒）→ 说明模型拿到的是几百字的空壳，快速判 `ok:false` |

根因：**小黑盒这类 SPA 服务端只返回一个空壳，正文全靠 JS 渲染。**
`tabs.onUpdated` 的 `status === 'complete'` 只代表壳和静态资源加载完，此时正文往往还没出来。

**修复（`src/background/page.js`）**：

1. `waitForStableContent()`：`complete` 之后继续轮询，**只要正文长度还在增长就继续等**，
   连续 `QUIET_MS`(1s) 不变且长度 ≥ `MIN_CONTENT_CHARS`(400) 才收工。
   注意返回的是**文本最长的那一次**，不是最后一次 —— 避免结尾抖动反而取到更短的。
2. `tryForegroundBoost()`：后台标签页可能被浏览器节流（不绘制、`IntersectionObserver` 不触发），
   导致靠滚动懒加载的正文永远不出来。此时把标签页**短暂切到前台**再等一轮，然后切回用户原来那一页。
3. `lowContent` 诊断位：正文 < 400 字时判定「没抓到正文」，
   **保留标签页**（不关），并把 `charCount` 透传到 UI。

**为什么把 400 字当分界**：实测空壳是 287 字；正常文章至少几百字。宁可放过去让模型判断，也不要因为误判而漏掉真实内容。

**UI 必须分开说两件事**（`sidepanel.js`）—— 这两种情况的下一步完全不同：

| 情况 | 提示 | 用户下一步 |
|---|---|---|
| `extract.lowContent === true` | 「这个页面没抓到正文」 | 切到那个标签页，用**「当前页面」入口**重试（手动打开的页面已经渲染好了） |
| `lowContent === false` 但模型 `ok:false` | 「这一页没有可消化的正文」 | 确认是不是登录页 / 错误页 |

**第三种情况：页面停在登录 / 权限校验界面**（2026-09-14 补）

上面两类之外还有一种被漏掉的：页面**本身**就是登录界面，正文真的只有十几个字。

踩到的场景：用户粘了学校统一身份认证页，UI 给的建议是「切到那个标签页用当前页重试 —— 手动打开的页面已经渲染好了，一定能抓到」。
**对登录页这是空头支票**：用户自己打开也只有登录界面，重试还是 17 个字，然后拿到同一段话。

修法：抓取侧报一个 `loginWall` 信号（`url` / `password` / `captcha` / `text` 四种原因），UI 分出第三种提示。判据命中任一即算：

| 判据 | 为什么可靠 |
|---|---|
| URL 含 `login` / `signin` / `cas` / `sso` / `passport` / `auth` / `verify` / `captcha` | 最省事也最准 |
| 有 `input[type=password]` | 密码框几乎只出现在登录、注册、权限校验页 —— 一篇讲登录流程的教程不会有密码框 |
| 有验证码输入框（`placeholder` / `name` / `id` 含「验证码」「captcha」「verify」「动态码」「短信」） | 同上，且**比正文关键词可靠得多** |
| 正文 < 300 字 **且** 含「登录/登陆/密码/验证码/帐号/账号/身份认证/扫码/注册」 | 兜底。「很短」这个条件不能省，否则一篇讲登录流程的长教程会被误判 |

UI 文案要落到「先登录」这一步，并明说「如果它本来就是登录页，忽略即可」—— 不要再让人去走「切过去重试」那条死路。

> 这是「两种情况的下一步完全不同，UI 必须分开说」这条原则的延伸：
> 当时只分了两类，登录页被硬塞进「没渲染出来」那类，于是给出了一条走不通的建议。

**验证方式（不需要浏览器）**：用 mock 的 `chrome` API 在 Node 里驱动 `page.js`，模拟三种页面行为 ——
① 正文自己渲染出来 ② 只有切前台才渲染 ③ 永远是空壳。三个场景 12 项断言全通过。
脚本同样放仓库外、跑完即删。

---

### 分类体系：按「用途」分 6 类，不按学科

阶段 2 引入自动分类。类别按**用途**分而不是按学科分 —— 因为收藏夹里本来就不都是 AI 技术内容
（用户明确收藏过游戏更新公告、应用推荐），按学科分会让一大半内容掉进「其他」，
而「其他」对用户没有任何价值。

| 分类 | 判据（读者读完会去做什么） |
|---|---|
| 技术学习 | 会去照着做、去学（**领域必须是软件 / 代码 / 系统 / 硬件**）|
| 工具与产品 | 会去装它、配它、用它 |
| 资讯与公告 | 只需知道有这么回事 |
| 游戏娱乐 | 主题是游戏 |
| 生活常识 | 跟身体或过日子有关（健康 / 饮食 / 家居 / 消费 / 出行） |
| 其他 | 兜底 |

**判序不能改**：先只看主题领域（是游戏就直接定「游戏娱乐」），再看「读完要做什么」。
这个顺序是实测定下来的 —— 不这样写，「游戏更新公告」会被按体裁判进「资讯与公告」；
「某组织发布新模型、公布跑分」会被判进「工具与产品」。两个都实测踩过。

#### 「技术学习」的判据要写正向特征，不能写否定式（2026-09-18 实测）

加「生活常识」时顺手发现一个**既有的误判**：一篇职场随笔（「换工作一年后踩过的三个坑」）
被判成「技术学习」。先做了 A/B 对照确认归因 —— **旧 prompt 也是 2/2 判错**，
说明与加分类无关，是判据本身的问题。

原来的判据是「可复用的技术知识、原理、架构、**做法、踩坑经验**」：
职场随笔里的「三个坑 + 经验教训」正好落进「做法、踩坑经验」，
而「技术」这个限定词太弱，模型直接忽略了。

| 尝试 | 写法 | 结果 |
|---|---|---|
| 1 | 加否定句：「这里的『技术』指软件、代码、系统、硬件 —— 讲职场、人际、情感、生活经验的，**哪怕写了「做法和教训」也不归这里**」 | ❌ 仍然判「技术学习」 |
| 2 | 换成**正向必要条件**：「判据是正文里**有没有具体的技术对象**：命令、代码、配置项、参数、报错信息、性能或容量数字、系统与架构的技术细节」 | ✅ 判「其他」 |

**结论：8B 模型对否定式约束基本不敏感**（「哪怕…也不归这里」它当没看见），
但对「有没有 X」这种**可检的正向特征**响应很好。

> 这和「prompt 里的『可选』等于『不做』」是同一条规律的两面：
> 模型执行的是"要什么"，不是"不要什么"。**写判据要写它该找什么，不要写它不该做什么。**

改完两侧都要回归 —— 加了「必须有技术对象」这个必要条件后，得确认纯讲架构思想、
没有命令代码的技术文章**不会**被推到「其他」。所以测试里专门加了一篇
《怎么设计一个能演进三年的后端架构》（通篇无代码），实测仍然归「技术学习」。

**分类与护栏的联合回归**（`wm-category-test.mjs`，9/9，约 83 秒）：

| 用例 | 期望 | 为什么用它 |
|---|---|---|
| 为什么一冷就流鼻涕 | 生活常识 | 新类要能命中（用户明确提到的那类） |
| Docker 多阶段构建 | 技术学习 | 回归：不能因为「也在讲原理」被生活常识吞掉 |
| 艾尔登法环更新公告 | 游戏娱乐 | 判序第一步不能被破坏 |
| 某实验室发布新模型 | 资讯与公告 | 披露技术细节也不该跑到技术学习 |
| 上线事故复盘 | 技术学习 | **技术类的踩坑复盘必须仍归技术学习**（这次给技术学习加了必要条件，最容易伤到的一侧） |
| 能演进三年的后端架构 | 技术学习 | **纯思想、无代码的技术文章不能被推到其他**（同上的另一侧） |
| 换工作一年后的三个坑 | 其他 | 被修掉的那条误判 |
| 笔记软件推荐 | 工具与产品（ok:true） | 护栏：推荐类内容必须可消化 |
| 统一身份认证 | ok:false | 护栏：登录页必须拦下 |

> 改 prompt 必须重跑这组 —— 里面既有分类判据的两侧边界，也有护栏的 ok:true/false。
> 只测「新加的类能命中」是不够的：**加一个类，最该担心的是它把别的类抢走。**

### 输出以完整为先：条数与长度由内容决定

| 分类 | 重点抓什么（不决定写多少字） | 为什么 |
|---|---|---|
| 技术学习 | 命令、参数、路径、配置项、执行顺序、踩过的坑 | 教程要能照着做 —— 省掉这些就等于没说 |
| 工具与产品 | 能做什么、关键能力或参数、适用场景、限制与代价 | 决定装不装、用不用 |
| 资讯与公告 | 谁发布了什么、改了什么、影响谁 | 读完只需知道 |
| 游戏娱乐 | 具体变化、具体数值、具体做法 | 要拿去用的 |
| 生活常识 | 具体做法、适用条件、容易记错或搞反的地方 | 要照着过日子用的 |
| 其他 | 值得记住的事实与结论 | |

**这是一次方向修正**。早期把产品定位成「压缩成一张能快速看完的卡片」，于是 prompt 给每个分类配了「目标字数」
（技术学习 200 字、资讯 80 字…），schema 把条数卡在 5 条、单条 220 字，`num_predict` 只有 1024。

阶段 2 用户反馈「输出不够、不完全，有的文章确实不是一句两句能说明白」。做了三组对照实验
（同一篇 B 站图文帖，正文侧分别是「只认前 8 张图」和「16 张全认」）：

| 实验 | 输入 | 输出 |
|---|---|---|
| A：8 张图 + 旧 prompt（用户当时看到的） | 4233 字 | 3 条 / 337 字，只讲到 Stage 0–2 |
| B：16 张图 + 旧 prompt | 8786 字 | 3 条 / 398 字 |
| C：16 张图 + 新 prompt | 8786 字 | **6 条 / 636 字**，五个阶段 + 两条路线 + 避坑指南全覆盖 |

A→B 说明「正文翻倍，输出只多 61 字」：**瓶颈是 prompt 的详略指令，不是 token 上限** ——
两次的 `done_reason` 都是 `stop`（模型自己写完了），且 `eval_count` 只有 212/255，离 1024 差得远。
所以修的是「要求」，容量只是为它配套放宽（见下表）。

| 参数 | 原值 | 现值 | 依据 |
|---|---|---|---|
| `num_predict` | 1024 | 3072 | 新要求是 8 条 × 400 字；实测中文约 0.7 token/字，1024 token 装不下 |
| `num_ctx` | 8192 | 16384 | 8000 字正文 ≈ 6100 token，加 system ≈ 1100，旧窗口只剩 1000 给输出。实测 16384 仅占 **5.85 GB** 显存（`ollama ps`）|
| `maxInputChars` | 8000 | 12000 | 16 张图的合并正文有 8786 字，旧上限会切掉尾部 |
| `points.maxItems` | 5 | **20** | 三次被真实内容顶上来，见下 |
| `points.itemMaxLen` | 220 | 400 | 教程要写清命令 / 参数 / 顺序，220 字经常不够 |
| `summary.maxLen` | 160 | 300 | 信息密集的长文需要两三句 |
| `requestTimeoutMs` | 120s | 240s | 输出变长后单次耗时约 24 秒（原来 5 秒）|

**条数上限的三次演进**（每次都是被真实内容顶出来的）：

| 上限 | 被什么顶掉 |
|---|---|
| 5 条 | 一篇 B 站《AI Agent 学习路线图》：五个阶段 + 两条路线 + 避坑指南，5 条装不下 |
| 8 条 | 一篇讲 11 个 skill 的公众号文章 —— **模型自己就输出了 11 条**（11 个章节一条不落），却被 `maxItems: 8` 判不合格、触发重试，最终妥协成 8 条。用户看到的就是「明明有 11 个却只讲 8 个」 |
| 20 条 | 兜底值（原文条目确实超过 20 时挑最值得关注的 20 条，并在 summary 里说明）。但这个上限没有解决**聚合类**内容 —— 见下一节 |

**教训：用固定的条数上限去卡「并列清单」这类内容，等于按自己的想象裁剪原文。**
模型在这类内容上的判断比上限可靠 —— 它会跟着原文的条目数走；该做的是**放宽上限 + 在 prompt 里写清「跟随原文条目数」**，
而不是让它妥协。回头看，5→8→20 两次都是「先定一个看着合理的上限，然后被真实内容打脸」。

另一个反直觉的数据：清单类内容每条天然简短（「是什么 + 干什么用」），
所以那篇 17 条的要点总字数（748 字）比原来 8 条的（636 字）**只多 112 字** —— 条数多不等于卡片臃肿。

上限仍然是上限：prompt 里明确写了「正文信息不够时如实写短，绝不注水凑字，也不要为凑数编造」。
回归验证：阮一峰周刊（6194 字）输出 5 条 / 403 字 —— 条数随内容增加，没有注水。

**对教程类，正反例写进 prompt 比抽象描述有效得多**（实测最立竿见影的一处改动）：

```
不合格（只是概括）：编写 Dockerfile，安装依赖并启动服务。
合格（能照着做）：Dockerfile 先 COPY requirements.txt 再 RUN pip install，
                  最后才 COPY ./app —— 顺序反了会导致改一次代码就重装全部依赖。
```

**校验刻意不做分类相关的分支**：schema 里 `itemMaxLen: 400` 是全局的防注水上限，不是按分类分档。
分类判错时如果连带把长度也判错，会产生假失败，而 8B 模型经不起多重硬约束 ——
详略梯度只写在 prompt 里引导，不进校验器。

**分类枚举在两处出现**（`shared/prompt.json` 的 system、`shared/output-schema.json` 的 `category.values`），
改一处必须同步改另一处。校验器对不在枚举内的值一律判失败并触发重试 —— 不许模型自由发挥出「AI技术」这类近义写法，
否则后续的归类统计和画像推断都会被污染。

**已知模型局限**：`Ollama` 会被稳定地写成「Oll、ama」（三轮打样出现三次，加「不要拆字」的指令无效）。
这是 8B 模型层面的问题，代码侧修不了 —— 归到阶段 3 的幻觉率指标里量化。

---

### 图文帖：干货在图里，必须上本地 OCR

阶段 2 用户反馈：**B 站动态「这个网站就不太行」**。

排查（无头 Edge + CDP 打开真实页面，注入真实 `content-script.js` 跑 `extract()`，再用真实 prompt 调本地模型）：

| 证据 | 结果 |
|---|---|
| 插件实际抓到的正文 | **1118 字**，全是「大家好，今天我要讲七个阶段」这类开场白 |
| 页面里的图片 | **16 张正经大图**（`opus-para-pic`，596×794 起） |
| 语义容器命中 | `article` / `main` / `[role=main]` 全为 0 → `pickRoot()` 退回 `document.body` |
| 模型输出的摘要 | 「分为 Track A（应用大师）和 Track B（建造宗师）两条路径，共 7 个阶段」—— 没说错，但**等于什么都没说** |
| 图里真正的内容 | 七个阶段怎么分、每阶段学什么用什么工具（aider / LangGraph / MCP / RAG）、Track A/B 各走哪条线 |

结论：**不是模型不行，是它只拿到了开场白。** 这类页面（B 站 opus、小红书笔记、公众号长图）
的正文是画在图里的，DOM 文字只是引子。

**方案：本地离线 OCR，把图里的字认出来拼进正文再消化。**

| 决策点 | 结论 | 为什么 |
|---|---|---|
| 引擎 | Tesseract.js + `chi_sim`（fast），原样放进 `vendor/tesseract/` | 项目承诺「全本地零云端」，不能为了识别几个字去连 CDN；约 5.6 MB |
| 跑在哪 | **侧栏**（扩展页面） | MV3 的 service worker 起不了 Worker、跑不了 WASM；侧栏同样是扩展页面，有 `host_permissions` 兜底，取跨源图片不受 CORS 限制 |
| 触发 | 正文 < 2000 字 **且** 正文区 ≥2 张大图 | 正常文章一张图都不识别。实测阮一峰周刊 6194 字 → 完全不触发 |
| 质量门 | 置信度 <55% 或字数 <8 → 整张丢 | 实测封面艺术字只有 33%（黑字压在带纹理的浅底上，二值化被骗）；流程图 87%、正文截图 90% |
| 去重 | 识别结果和 DOM 文字重复的丢掉 | 图文帖常把同一段正文再截一张图（实测该帖第 3 张就是） |
| 上限 | 一页最多 30 张，拼进正文最多 9000 字 | 识别一张约 0.8 秒（16 张实测 13 秒）。额度用满即停手，并在 UI 里说明停在哪里 |

**张数上限从 8 提到 30**（起因见下）：那篇动态有 16 张正文图，只认前 8 张时
**收 6 张 / 2546 字**，全部认完则是 **收 14 张 / 6260 字** —— 第 9～16 张每张置信度 85~91%、400~540 字，
全是正文，被上限整段丢掉，合计少喂 **3714 字**。而 16 张全认只要 13 秒。

**实测效果**（同一篇）：正文 **1118 字 → 7378 字**，摘要从「两条路径」
变成 6 条要点，覆盖五个阶段、两条路线与避坑指南。

**⚠️ 三个必须照抄的配置**（少一个都跑不起来，且报错信息全部指向错误的方向）：

| 配置 | 不这么写的后果 |
|---|---|
| `workerBlobURL: false` | tesseract.js 默认 `new Worker(URL.createObjectURL(blob))`，MV3 的 `script-src 'self'` 不允许 blob: worker。报 `Failed to execute 'importScripts' on 'WorkerGlobalScope'`，且**错误信息里写的是 workerPath 那个文件加载失败** —— 极其容易误判成路径写错，实际是默认参数踩了 CSP |
| `corePath` 指向具体 `.js` 文件 | 指向目录时 worker 会按浏览器能力拼四个候选名（`relaxedsimd-lstm` / `simd-lstm` / …），只放一个文件必然 404 |
| `manifest.json` 加 `'wasm-unsafe-eval'` | MV3 默认 CSP 禁止 `WebAssembly.compile` |

另：`tesseract-core-*-lstm.wasm.js` 是 emscripten SINGLE_FILE 构建（wasm 以 base64 内嵌），
**不需要**再放配套的 `.wasm`（实测删掉后结果不变），省 2.9 MB。

**这是一次「不为了解决问题而装新软件」的例外**：引擎是随仓库走的静态文件，用户侧零安装、零联网。

**回归用例**：
| 页面 | 期望 |
|---|---|
| B 站 opus 图文帖（正文在 16 张图里） | 触发 OCR，抓到的字数显著上升，摘要含具体阶段 |
| 普通长文（>2000 字） | **不触发** OCR，行为与阶段 1 一致 |
| 只有装饰图 / 艺术字封面的短页 | 触发 OCR 但全部被质量门丢弃 → 报「没抓到正文」并说明图片也没认出来，**不硬着头皮喂开场白** |

端到端跑法：无头 Edge 加载真实扩展 → 打开真实侧栏页面 → 设 `#url-input` 值 → 点 `#btn-digest-url` → 轮询 `#result-body`。
走的是 `sidepanel.js / service-worker.js / page.js / content-script.js / ocr.js` 全套代码 + 真实本地模型，脚本放仓库外、跑完即删。

---

## 四、代码约定

- **原生 JS 起步，暂不引入构建工具**；先跑通再工程化。
- `service worker` 会被浏览器随时回收 —— **状态一律落 `chrome.storage`，不要依赖内存变量跨事件存活**。
- **侧栏是扩展页面，可以直接 `import` 仓库里的 ES module**（`sidepanel.js` → `ocr.js` → `vendor/tesseract/*`），
  重活（WASM / Worker / 长循环）放侧栏，service worker 只做消息路由。
- `manifest.json` 的 CSP 必须保留 `'wasm-unsafe-eval'` —— 去掉它扩展能装，但 OCR 一起 worker 就挂。
- 权限最小化：用 `scripting` **运行时按需注入**，不写静态 `content_scripts`。
  但 **`host_permissions` 必须含 `*://*/*`**，不能只靠 `activeTab` —— 原因见下节。

### 为什么必须放 `*://*/*`，不能靠 `activeTab`

阶段 0 实测踩到：侧栏点「抓取正文」报
`Cannot access contents of url "..." Extension manifest must request permission to access this host`。

根因两条，都是结构性的，不是配置笔误：

1. **`activeTab` 是"一次性"授权**：只在用户点扩展图标那一刻授予，且扩展 UI 常驻时第二次调用就失效。侧栏恰恰是常驻 UI，用户何时点抓取不可预测 —— 这个组合天生不稳。
2. **`activeTab` 覆盖不到"收藏夹里的链接"**：那根本不是当前标签页。阶段 1 的核心输入就是收藏链接，`activeTab` 从设计上就用不上。

**结论**：`host_permissions` 必须包含 `*://*/*`。

**后续可选的工程化方向**（未做，别当成已完成）：改成 `optional_host_permissions` + `chrome.permissions.request()`，
由侧栏按钮手势触发按需申请。好处是安装时不显示「读取所有网站数据」，权限叙事更干净；
代价是引入「未授权 / 已授权 / 被拒」三态 UI。留到阶段 4 打磨期再评估。
- Ollama 调用统一走 service worker，侧栏只通过 `chrome.runtime.sendMessage` 通信。
- 结构化输出：Ollama `format: json` + 解析校验 + 失败重试（有次数上限）。

---

## 五、工作方式

1. **按阶段交付**，每阶段完成后停下等验证，不要一次生成整个项目。
2. 遇到未拍板的取舍，**给 2 个具体方案并说明推荐哪个**，不要列抽象选项。
3. 输出用表格 / 结构化列表，少写长段落。
4. 每完成一个可运行的改动，**提醒用户单独提交并推送**。
5. 任何结论标注：哪些是**已验证事实**，哪些是**推断**。

---

## 六、当前阶段

**阶段 1 · 最小闭环 —— ✅ 已完成并通过验收**（Edge 实测，2026-09-12）。
期间修掉四个真问题，全部记在上文专节：`activeTab` 与侧栏不兼容、`OLLAMA_ORIGINS` 403、护栏边界划错、SPA 正文抓取时机。

**阶段 2 · 完整功能 —— 进行中。**

| 步 | 内容 | 状态 |
|---|---|---|
| 2.1 | 分类体系 + 自动归类 + 按分类分派详略 | ✅ 已完成并通过验收 |
| 2.2 | 落库（IndexedDB）+ 同 URL 去重 | ✅ 已完成并通过验收 |
| 2.3 | 归档浏览 + 关键词搜索 | ✅ 已完成（待浏览器验收） |
| 2.4 | 批量消化：10~20 条 → 排序 + 本周必看 3 条 | ✅ 代码完成（待浏览器验收） |
| 2.5 | 画像推断：从已消化内容聚合出关注主题 / 学习阶段 | ✅ 代码完成（待浏览器验收） |
| 2.6 | 可行动清单 | ✅ 已完成（待浏览器验收） |
| 2.7 | 图文帖 OCR：正文在图片里的页面（B 站动态等）先把图里的字认出来再消化 | ✅ 已完成并通过验收 |

阶段 2 的验收标准（任务书）：一批链接（≥5 条）→ 分类 + 摘要 + 观点 + 个性化清单，可搜索。
→ **已全部达成**（2.3 是最后一块）。

阶段划分与验收标准见 `README.md` 的阶段进度表。

### 已落地的结构

```
shared/prompt.json           prompt 模板 + 调用参数（唯一来源）
shared/output-schema.json    输出结构与校验规则（唯一来源）
vendor/tesseract/            离线中文 OCR 引擎（worker + WASM 核心 + 语言包，见其 README）
src/background/shared.js     加载 shared/、模板渲染、结构校验
src/background/llm.js        Ollama 探活与 /api/chat
src/background/digest.js     消化流水线（解析 → 校验 → 重试 → trace）+ 输入形态判定
src/background/page.js       标签页与抓正文（含图文帖判据）
src/background/store.js      本地归档库（IndexedDB + 同链接去重的归一化规则 + 搜索分页 + 分类版本标记）
src/background/service-worker.js  消息路由 + 落 storage + 归档
src/content/content-script.js     抓正文与正文大图
src/sidepanel/ocr.js         图片文字识别（跑在侧栏，不占 service worker）
src/sidepanel/batch.js       批量消化的编排（同样跑在侧栏，理由见对应小节）
```

**改 prompt / 模型参数 / 校验规则，一律改 `shared/` 下的 JSON，不要写进 JS。**
阶段 3 的 Python 评测脚本会读同一份文件 —— 两边必须对同一份 JSON 解释一致。

`attempts[]` 里每次尝试的原始输出与错误原因是阶段 3 失败分类的唯一数据来源，**不要为了省空间把它删掉**。

### 2.2 落库：去重键怎么定的

**为什么用 IndexedDB 而不是继续用 `chrome.storage.local`**：后者是纯 KV，
存 `lastDigest` 这种单条状态合适，但归档要按时间列、按分类筛、后面还要搜 ——
KV 得把整个数组读出来自己筛。IndexedDB 有索引、能游标分页，且不需要额外权限。

**去重键 = 归一化后的 URL**，剥掉的全是「同一个页面」的不同写法：
协议、主机名大小写、默认端口、末尾斜杠、锚点（`#` 只是页内位置）、跟踪参数（且排序）。

三条保守取舍，理由都写在这里，改之前先读：

| 取舍 | 理由 |
|---|---|
| 跟踪参数过滤**按域名限定**（`from` 只在 B 站剥） | `from` 在别的站点可能有实际含义，全局删会误合并 |
| **有内容的参数一律保留**（`?id=1` ≠ `?id=2`） | 漏合并只让人看到重复，误合并会真的丢内容 —— 两个方向的代价不对称，宁可少合并 |
| **`www.example.com` 与 `example.com` 不合并** | 它们不是协议层面的等价，去 www 属于猜测 |

实测：`.../opus/123?from=search&spm_id_from=333.337.0.0` 与裸链接判为同一篇，
覆盖而非新增（`digestCount` 累加、`firstDigestedAt` 不动、库中总数仍为 1）。
`saveDigest` 的「先读再写」分两次事务，单看有竞态，但消息路由是串行处理的，
实际不会并发到同一个键上 —— 这里不引锁，保持简单。

**只归档模型判 `ok:true` 的结果。** 判定「没有可消化正文」的页面不入库，
否则登录页 / 错误页会污染后面的画像。

### 聚合类内容：该不该归纳，由代码判定，不交给模型

**问题**（2026-09-15 实测，阮一峰周刊第 296 期，6194 字）：

| 项 | 值 |
|---|---|
| 模型第一次输出 | **46 条**（把周刊里几十条资讯逐条罗列） |
| schema 上限 | 20 条 → 判不合格 |
| 后果 | 触发整轮重试，`41.6s → 74s`；重试后妥协成 19 条 |

根因是 1.4.0 那条判准写得太宽：「原文是并列清单时条数跟着 N 走，一项都不能漏」。
它对「一篇文章里列了 N 个要点」是对的，但被套用到了**周刊 / 日报 / 合集**这类
「一份文档装了 N 条彼此无关的内容」上 —— 那种内容该按主题归纳。

**用户拍板**：单一主题的并列清单才跟随条目数，聚合类按主题归纳成若干条。

#### 第一次尝试：把规则写进 prompt —— 失败

在 prompt 里补了「聚合类要按主题归纳、条数等于组数」的描述性要求，同一篇周刊跑 4 次：

| 条数 | 耗时 |
|---|---|
| 16 | 41.1s |
| 45 | 65.9s |
| 42 | 62.8s |
| 46（改之前） | 41.6s |

**结论不是「模型不会归纳」，而是「它判断不出该不该归纳」** ——
周刊的条目在形式上就是并列清单，和「一篇文章列了 11 个要点」长得一模一样，
8B 模型区分不了这两种。描述性指令对它基本等于没写。

#### 真正的解法：把判断从模型手里拿走

对照实验 —— 在 system 开头明确写一句「这份正文是聚合类」：

| 条件 | 条数 | 耗时 |
|---|---|---|
| 不给信号（4 次） | 16 / 42 / 45 / 46 | 41~66s |
| **明确告知类型** | **10 条** | **27.8s** |

一次就解决。于是加 `detectShape()`（`src/background/digest.js`）：

- 按标题里的**刊物特征**判定：`周刊|周报|日报|月报|第 N 期|合集|盘点|汇总|要闻|weekly|newsletter|roundup`
- 标题看不出时看正文**开头 200 字**（有些站点的标题被取成了站名）
- 结论渲染成一句明确的话塞进 system（`{{shapeRule}}` 占位符，文案在 `shared.js` 的 `shapeRuleFor()`）

**判据要「准」不要「宽」** —— 这里的方向和登录墙检测相反：

| 方向 | 后果 |
|---|---|
| 漏判 | 回到罗列 → 超上限 → 白跑一轮重试，**但信息不丢** |
| 误判 | 单一主题被强行归并，**真的丢信息**（正是用户抱怨过的问题）|

实测踩过：加过「一览」之后，《React 19 的新特性一览》这种正常文章标题被误判成聚合类。
所以只收「一说出来就说明是刊物」的词。

#### 验证

`wm-llm.mjs` 直接 import 生产代码的 `detectShape` / `shapeRuleFor`，不抄副本：

| 内容 | 形态 | 条数 | 耗时 |
|---|---|---|---|
| 阮一峰周刊（收录 28 篇） | `aggregate` | 17 条（归纳，摘要里交代原文规模） | 32.9s |
| 公众号《11 个必装 Skills》 | `single` | 18 条（逐条，一条不漏） | 23.3s |

形态判定的 10 组用例全过（含《React 19 的新特性一览》这类易误判的）。

> 顺带解锁一个做法：仓库没有 `package.json`，Node 默认把 `.js` 当 CommonJS。
> 加 `node --experimental-default-type=module` 就能直接 `import` 生产模块 ——
> 验证脚本因此可以复用真实判定逻辑，不必在脚本里抄一份会漂移的副本。

### 批量消化：编排为什么放在侧栏（2.4）

**最大的约束不是模型，是 MV3 的生命周期。** 一批 10 条要跑 4 分钟左右，
而 service worker 空闲几十秒就会被浏览器回收 —— 把循环写进 SW，
跑到第三条就会被掐断，用户还完全不知道为什么停了。

所以分工是：

| 谁 | 负责什么 | 为什么 |
|---|---|---|
| 侧栏（`src/sidepanel/batch.js`） | 循环、进度、续跑 | 页面只要开着就不会被回收 |
| service worker | 只做「消化一条」「排序一批」两个短动作 | 每个动作都短，被回收也不影响已完成的部分 |

**续跑不需要额外的状态机**：每条跑完都已经落进 IndexedDB，
所以重开侧栏再点一次，已消化的会被直接复用 —— 这就是从断点续上，
不必单独维护「上次跑到第几条」。

**复用窗口 30 天**：批量要的是「横向排序」，不是重读原文。已消化的直接复用，
所以第二次跑同一批几乎不用等。

**单条失败不打断整批**：10 条里坏 1 条，剩下 9 条照样该排序。
失败的单列出来说明原因，且不混进排序的输入。

**排序只喂「标题 + 分类 + 摘要」，不喂正文**：10 条正文合起来会撑爆上下文，
而排序本来也不需要全文判断 —— 这也顺带保证了它是对**已消化结论**的二次判断，
不是重读一遍原文。

**排序依据**（用户拍板）：相关性 > 可操作性 > 时效。
定义写在 `shared/prompt.json` 的 `batchSystem` 里，与其余 prompt 一样是单一来源。

**结果里带一个 `repaired` 标记**：模型给的 `order` 可能越界、漏项、重复 ——
这些都是 schema 表达不了的（要拿 `items.length` 去比），所以在 `rankBatch` 里
补了一道业务校验，并把「是否补过 / 裁过」记下来。阶段 3 的失败率指标要这个数。
漏掉的序号按原顺序补在后面：**宁可顺序不完美，也不能让用户丢内容**。

**验证方式**（三个脚本都在仓库外，按约定不进仓库）：

| 脚本 | 覆盖什么 | 结果 |
|---|---|---|
| `wm-rank-test.mjs` | 真实模型跑排序：结构自检 + 前 3 条是否都属于相关分类 | 一次成、16.8s、结构自检通过 |
| `wm-batch-test.mjs` | 编排的 8 组分支：归档复用 / 归档过期 / 查询抛错 / 单条失败 / 全部失败 / 中途停止 / OCR 分叉 / 排序失败 | 18/18 |
| `wm-batch-ui.mjs` | 面板接线：元素齐全、切页可见、空收藏夹有明确提示 | 12/12 |
| `wm-batch-view.mjs` | 结果交互：刷新收藏夹列表、可展开条目、原文链接、不覆盖「最近结果」 | 18/18 |
| `wm-result-pane.mjs` | 结果卡片与面板的归属：切「批量」收起、切回其他面板恢复（注入假结果 + 重载，不用真跑模型）| 8/8 |
| `wm-collapse-test.mjs` | 结果卡片可收起：默认值分叉（还原 vs 新结果）、偏好记忆、切面板不干扰 | 17/17 |
| `wm-action-test.mjs` | 行动清单模型层：`sanitizeRefs` 单测 + 真实模型「画像 → 清单」 | 23/23 |
| `wm-action-ui.mjs` | 行动清单界面层：初始空状态、归档为空的真实错误路径、注入假清单验渲染与还原 | 17/17 |
| `wm-archive-test.mjs` | 归档搜索：存储层 14 项 + 界面层 16 项 + 分页回归（同一毫秒写入不漏） | 34/34 |
| `wm-category-test.mjs` | 分类 6 类 + 护栏：新类命中、两侧边界、回归「别的类不被抢走」 | 9/9（约 83s，真实模型）|
| `wm-reclassify-test.mjs` | 重新分类：编排层的 force / skipRanking、版本落后的筛选、界面提示行与单条重跑入口 | 17/17 |

#### 结果卡片的默认展开状态，按「谁触发的渲染」分叉

用户反馈：「给消化结果也加一个可以收起的功能，不然每次要找下面的东西比较麻烦。」
（结果常常有十几条要点，展开着会把「我的画像」「本地模型」都挤到屏幕外。）

改成和其它卡片一致的原生 `<details>`，但**默认值不能只有一个**：

| 触发时机 | 默认 | 为什么 |
|---|---|---|
| 刚消化完（用户点了按钮） | **展开** | 那是他刚操作的结果。收起来他会以为没出结果 |
| 重开侧栏还原上次结果 | **按用户上次的选择**（首次为收起） | 那不是本次操作的反馈，展开着照样挡路 |

所以 `renderResult()` 把这件事交给调用方（`{ collapsed }`），自己不猜默认值。
两个时机如果都写死同一个默认值，必然会得罪其中一边。

两个实现细节值得记：

- **偏好绑在 `summary` 的 `click` 上，不是 `details` 的 `toggle`**。
  程序设置 `open` 也会触发 `toggle`，那样「新结果自动展开」会顺手把偏好改写成「展开」，
  用户下次重开看到的就是永远展开 —— 想修都难查。`click` 只由用户交互触发。
  顺带：`click` 发生在状态切换**之前**，所以取值要写 `!card.open`。
- **收起时标题右侧要有东西**。只有一个「消化结果」的标题，用户看不出里面是哪一篇，
  就不敢一直收着。那里放来源标题（单行省略），完整来源（含抓到的字数）留在折叠区内。

验证脚本刻意用**本地起页面的真实消化**跑第二段（不依赖外网），
因为「新结果自动展开」这条只有真跑一次才能验；用假结果注入只能验还原那半条。

#### 结果为什么是「可展开」而不是平铺

第一版把每条都平铺出来（标题 + 分类），「本周必看」还把前三条重复列一次 ——
一批十几条时面板被撑得很长，反而看不出该看哪条。

改成：

- 每条**默认收起，点标题才展开**这条的摘要与要点
- 「为什么必看」**常驻显示**在标题下 —— 它是这一批的核心产出，不该也被收进折叠里
- 每条右侧挂「原文 ↗」，**放在 `<details>` 外面** —— 不展开也能直接跳走

折叠用原生 `<details>`：不引入 JS 状态，还自带键盘可达性。

#### 两个「会让人误解」的细节

**切到「批量」面板那一刻就收起单条消化的结果卡片**（在 `switchPane` 里）。

第一版只在「点开始批量」那一刻收起 —— 用户反馈「转到批量的时候，粘贴链接的消化结果
还跟着过来」。其实**切换面板那一刻**就该收：那是单条消化的产物，和批量无关。

切回其他面板时按 `resultBody` 有没有内容自动恢复 —— 不能让用户以为上次的结果丢了。

**批量不写「最近一次结果」。** `digestAndStore` 默认会把结果写进
`chrome.storage.local.lastDigest`（侧栏重开时靠它还原）。批量跑完一批再重开侧栏，
看到的却是其中**最后一条**的单条结果 —— 会让人以为整批只消化了一条。
所以批量路径带 `quiet: true` 一路传下去（`DIGEST_URL` →（OCR 分叉）→ `DIGEST_TEXT`），
跳过这次写入。

#### 一个边界：批量不足 3 条时

`mustRead` 原本写的是 `exactLen: 3` —— 但批量只有 1~2 条时模型给不出 3 条，
schema 会判它不合格，于是白跑满 3 次重试再整批失败。
改成 `minItems: 1, maxItems: 3`：**prompt 仍然要求 3 条，schema 只负责不把它卡死**。
（`shared/output-schema.json` → 1.5.1）

### 画像：数字交给代码，模型只给定性

**输入只取最近 40 条的「标题 + 分类 + 摘要」**，不喂正文、也不喂要点：
判断「在关注什么方向」用摘要就够，而几十条的要点合起来会撑爆上下文。
更早的内容也不取 —— 那是「当时在关注什么」，已经过时了。

**数量、占比这类数字一律不交给模型。** 它在计数上不可靠，而且**算错了没人看得出来** ——
用户会把「RAG 占 40%」当成事实记住。所以模型只输出定性的方向名与说明，
分类分布由 service worker 拿归档库的真实数据统计（`byCategory`）；
UI 上还特意标一句「直接统计，不是模型估的」，两者分开显示。

**位置拆成「枚举 + 依据」两个字段**（`level` + `stageReason`）—— 这是实测定下来的：

| 写法 | 实测结果 |
|---|---|
| `stage`：一句话说明读者现在的位置 | 模型永远只写两三个字（「进阶实践者」），而且 schema 拦不住 —— 4 个字就够长了，它总能蒙混过关。连跑两次都触发了重试 |
| `level`：枚举（入门 / 进阶 / 实战 / 说不准）+ `stageReason`：至少 15 字的依据 | **一次通过**，而且真的写出了推理（「内容涉及源码分析、架构讨论与实战实现，如 RAG 检索器、Agent 框架…」）|

教训和「聚合类该不该归纳」那次是同一个：**能结构化的就别让它自由发挥**。
给它一句话让它写，它会用最短的方式糊弄；给它一个枚举 + 一段必须写长的依据，它反而认真。

**少于 5 条直接不给结论。** 三五条内容推出来的「关注方向」纯属碰运气，
而用户会把它当成事实。这时返回一句能照着做的提示（「用批量攒够再来」），比硬编一份画像诚实。

**验证**（`wm-profile-test.mjs`，两层共 18/18）：

- 模型层：直接 import 生产的 `buildProfile` 跑真实模型，断言结构、枚举合法性、
  「没有编造百分比」、「方向名不是分类名」
- 界面层：往 `chrome.storage` 注入一条假画像 + 重载，验卡片的渲染与还原（不重跑模型）

另外补了 `wm-validate-test.mjs`：把几套校验器（digest / batch / profile）都回归一遍 ——
把 `validateBatch` 抽成 `validateSection` 时，最容易伤到的恰恰是旁边那个「没动过」的 `validateDigest`。
（后来 2.6 又给它加了 action 段，见下。）

### 行动清单：整条链路的终点

前面每一步产出的都是「对内容的理解」，只有这里产出「对下一步的建议」——
它和「可量化的失败率评测」一起构成产品差异化（见「一、项目定位」）。

| 取舍 | 为什么 |
|---|---|
| 每条动作必须有 `how`（第一步，≥15 字） | 「加强 RAG 方面的学习」这种话没有信息量。要求写到「今天晚上打开电脑就能开始」的程度：建什么文件、跑什么命令。验证脚本里带一份套话黑名单（`制定学习计划 / 系统学习 / 多看多练 / 加强学习 / 打好基础`…），实测输出没命中 |
| `kind` 用枚举（动手做 / 补一块 / 读一篇 / 整理） | 理由和分类体系一样：模型自造的「学习类」在界面上没法配颜色，也没法统计 |
| `refs` 是**可选**字段 | 不是每条动作都对应某篇收藏，硬要求会逼它编。为此给 `validateField` 加了 `optional`（空数组等同省略） |
| 序号范围由代码过滤（`sanitizeRefs`） | schema 只能校验「它是数字数组」，管不了范围。序号错一个就指向不相干的内容，而且**错得很隐蔽** —— 界面上只会显示一篇牛头不对马嘴的文章。所以越界 / 非整数 / 重复的一律丢掉：**宁可少给也不能给错** |

**`refs` 的说明改了两次，这是本次最有价值的一次修正。**
第一版写的是「可选。如果跟某几篇直接相关就给出序号，没有就省略」——
实测模型**一个引用都不给**，包括「读一篇」那种逻辑上必须给的（它甚至在 `how` 里写
「从收藏中选择一篇关于 RAG 性能调优的实战文章」，就是不给序号）。
改成「『读一篇』时**必须给** + 带具体出处的建议比空泛的建议有用得多 + 给一个例子」之后，
一次给出 6 个引用，且全部指向相关条目。

> 教训：8B 模型对「可选」的理解就是「不做」。如果某个字段在业务上是必需的，
> 就必须在 prompt 里把「什么时候必须给」写清楚，否则等于没写。

**清单依赖画像，但不要求用户先点画像。** 没有画像时 `buildActionsNow()` 先算一份
（复用 `buildProfile`）并落库，侧栏的画像卡片跟着刷新 —— 用户点的是「生成清单」，
不该先被要求去点另一个按钮。返回里带 `profileStep`（`built` / `reused`）让 UI 知道要不要刷新画像卡片。

**`validateField` 的 `optional` 是这次唯一动到的公共代码**，它会把四套结构都碰到 ——
所以 `wm-validate-test.mjs` 扩到 25 项，digest / batch / profile / action 全部回归，
并专门加了一条「optional 没有放宽普通字段」（digest 的 summary 缺失仍然要报错）。

**验证**：

| 脚本 | 覆盖 | 结果 |
|---|---|---|
| `wm-action-test.mjs` | 模型层：`sanitizeRefs` 单测（越界 / 0 / 负数 / 小数 / 非数字 / 非数组）+ 真实模型跑完整「画像 → 清单」 | 23/23 |
| `wm-action-ui.mjs` | 界面层：初始空状态、归档为空时的**真实错误路径**（走真实后台，不注入）、注入假清单验渲染与还原 | 17/17 |
| `wm-validate-test.mjs` | 四套校验器回归 | 25/25 |
| `wm-rank-test.mjs` | 批量排序没有被 `optional` 改动影响 | 通过 |

界面层的「归档为空」那一段值得留着：它验的是**真实的错误路径**而不是注入的假象 ——
后台确实返回了「归档里只有 0 条，太少…攒够 5 条以上再来」，UI 也确实把它渲染成了
能照着做的提示。这类「正常但无结果」的状态最容易被写成一句干巴巴的「失败」。

### 归档搜索：一个只有翻到第 31 条才会暴露的 bug（2.3）

**这个 bug 是测试抓出来的，而它在界面上几乎不可见。**

`digestedAt` 是 `Date.now()`。批量消化（以及任何连续写库的场景）**很容易在同一毫秒
写进好几条** —— 而这些记录的时间戳完全相同。

分页续读原本的条件是「严格小于上一页最后一条的时间」：

```js
IDBKeyRange.upperBound(before, true)   // before 是 digestedAt
```

后果：**时间与上一页末条相同的记录会被整段跳过**。实测 35 条里只翻出 34 条 ——
少一条，用户不会发现，也不会有人报告。这类「静默丢内容」比崩溃难查得多。

修法：给库加一个**复合索引 `[digestedAt, key]`**（`DB_VERSION` 1 → 2），游标
按它排序，续读条件变成严格小于 `[时间, 主键]`。每条记录的主键唯一 → 索引键唯一 →
游标有全序 → 不重不漏。

两个连带的东西：

- `onupgradeneeded` 要**兼容已有库**：v1 升 v2 时对象仓库已存在，不能再
  `createObjectStore`（会抛「已存在」），要 `req.transaction.objectStore(STORE)` 拿回来补索引；
  并且每个索引都要 `indexNames.contains` 判断，否则重复创建同样会抛。
- 加 `req.onblocked` 的日志：升级被另一个开着旧版侧栏的连接挡住时会一直等，
  不写日志就只能看到「归档打不开」。

其余取舍：

| 取舍 | 为什么 |
|---|---|
| 搜索在本地做，不经过模型 | 关键词匹配花 20 秒等模型没有道理。语义搜索是另一个量级的事，先不做 |
| 只搜**标题 + 摘要**，不搜 `points` | 要点动辄几百字，搜进去会让「正文里顺带提了一句」的内容大量命中，噪音换来的召回不划算（验证脚本里有一条专门盯这个：造一条只在 points 里出现的词，断言搜不到）|
| 摘要与要点**随列表一起返回** | 展开是瞬时的，不必再请求一次。代价只是多传两项，而它们本来就是用户要看的东西 |
| 输入防抖 250ms **+ 请求序号** | 防抖只保证「不在打字途中狂发请求」；用户停一下再改一个字时，两个请求会同时在飞，先发的不一定先回。所以每次请求带序号，回来时对不上就丢弃 —— 否则界面会显示上一个词的结果 |
| 没有分类的老记录归入「未分类」 | 阶段 1 落下的记录没有这个字段，得能单独筛出来 |

**验证**（`wm-archive-test.mjs`，34/34）：

- 存储层（在扩展页面里直接 `import store.js`）：关键词命中标题 / 摘要、**不命中只在
  要点里的词**、大小写不敏感、分类筛选、未分类、关键词 + 分类组合、无命中返回空数组、
  三页拼起来不重不漏
- 界面层：切标签页、输入即搜、换分类、点开展开（摘要 + 要点）、原文链接的 href/target、
  无命中时的可操作提示、35 条翻两页拿到全量
- **分页回归**：直接往 IndexedDB 写 5 条 `digestedAt` 完全相同的记录，按每页 2 条翻，
  断言 5 条一条不漏 —— 这就是上面那个 bug 的定点回归（用它验过修复前后的差异）

> 顺带：`DB_VERSION` 升级是有风险的动作（用户库里已经有数据）。改完特意跑了一次
> **真实消化**的用例（`wm-collapse-test.mjs`，17/17），确认 v2 库上的写入、归档、
> 结果卡片渲染都正常 —— 只测新功能不测老链路，是这类改动最容易翻车的地方。

### 分类体系改了，老记录怎么跟上（2026-09-18 补）

用户要求「加一个重新分类吧，以后更新也方便」。难的不是那个按钮，是**怎么知道哪些该重跑**。

#### 做法：让记录自己带版本号，不引入任何外部状态

归档记录多存一个 `promptVersion`（写库时用的是哪版分类体系）。于是：

| 事件 | 结果 |
|---|---|
| 改了 prompt 并升版本号 | 所有老记录自动变成「待更新」，不需要任何迁移脚本 |
| 重跑过一条 | 它写入新版本号，自动从「待更新」里消失 |
| 用户中途关掉侧栏 | 下次再点，**已经跑过的会被自动排除** —— 天然的断点续跑，不需要状态机 |

这一点是关键：库大了之后全量重跑要十几分钟，不可能要求用户一次跑完。
换成「维护一份进度列表」的写法，就会多出「进度存到一半被杀」这类边界，
而按数据自身判断则完全绕开了它们。

顺带覆盖了一个历史遗留：**阶段 1 落下的老记录根本没有 `promptVersion` 字段**，
拿到的是空串，与当前版本不等 —— 于是它们会被自动纳入第一次重跑。

#### 编排直接复用批量那条循环

`runBatchDigest()` 加了两个选项，语义清晰、互不干扰：

| 选项 | 作用 | 为什么重新分类需要它 |
|---|---|---|
| `force` | 跳过「查归档 → 命中就复用」那一段 | 复用旧结果等于没重跑 |
| `skipRanking` | 跑完不调 `RANK_BATCH` | 重新分类要的是「分类被校正」，不是「排出优先级」，省一次模型调用 |

> 这两个选项也顺带证明了当初把编排写成注入式（`send` / `onProgress` / `shouldStop`
> 全从外面传）是值得的 —— 复用它的成本就是加两个默认参数。

#### 界面上的两处

- **面板顶部一行 + 一个按钮**，且**只在有过期记录时才出现**（平时不占位置）。
  提示里写清条数与大概耗时 —— 用户要判断的是「现在点不点」，不是「有多少条」。
- **每个条目的展开区里有「重新消化」**，用于只想校正某一条的时候。
  按钮文案在列表重建后会被冲掉，所以结果写在下方的提示行里。

#### 验证（`wm-reclassify-test.mjs`，17/17 + 一次真实端到端）

| 层 | 验什么 |
|---|---|
| 编排层（纯逻辑，不启浏览器）| 不传 `force` 会走归档复用、传了**不查归档且真的重跑**；`skipRanking` 不调排序、返回 `ranked: null`；**不传 `skipRanking` 时照常排序**（默认行为不能被带偏）|
| 存储层 | 只挑出版本落后的；**没有版本号的老记录也算过期**；当前版本的不重复处理 |
| 界面层 | 有过期记录才出现提示行、文案含条数与耗时、条目里有「重新消化」、**全部更新后提示行自动消失** |

真实端到端另跑了一次（临时脚本跑完即删）：一条记成「其他」的打嗝科普，
点「重新消化」→ 15 秒后变成「生活常识」，摘要被更新（不是复用旧结果），
版本号从 `1.9.1` 刷新到 `1.10.2`，待重跑数归零。这条路径正是用户会走的。
